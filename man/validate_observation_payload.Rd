% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phone_observations.R
\name{validate_observation_payload}
\alias{validate_observation_payload}
\title{Validate Observation Payload}
\usage{
validate_observation_payload(
  feature_payload,
  device_settings,
  media_dir = NULL
)
}
\arguments{
\item{feature_payload}{List. List of feature records created with \code{build_feature_record()}.}

\item{device_settings}{List. Device settings created with \code{build_device_settings()}.}

\item{media_dir}{Character or NULL. Path to directory containing media files.
Required if any observations have media types.}
}
\value{
A list with \code{$valid} (logical) and \code{$errors} (character vector).
}
\description{
Validates the device settings and feature payload before submission to the API.
Checks for required fields, valid item types, and verifies media files exist.
}
\examples{
\dontrun{
  validation <- validate_observation_payload(
    feature_payload = my_features,
    device_settings = my_device,
    media_dir = "/path/to/media"
  )

  if (!validation$valid) {
    stop(paste(validation$errors, collapse = "\n"))
  }
}

}
\author{
Adam Varley
}
